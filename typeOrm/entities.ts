import { User } from "src/users/user.entity";
import { Fee } from "src/fees/fees.entity";
import { Invoice } from "src/invoice/invoice.entity";

const entities = [User, Fee, Invoice];
export default entities;
